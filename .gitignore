/target
**/*.rs.bk
/.idea
/.vscode
*.local.toml
/spec.pdf
# Prebuilt binary staged into the Complement image build context by CI.
/docker/complement/saltator

# Interop test scratch: CA, per-server certs, and Synapse's generated state.
/docker/interop/ca/
/docker/interop/certs/
/docker/interop/synapse-data/
__pycache__/
/.cargo/

# Admin console: node_modules, and the bundle staged into the embed crate.
# The bundle is built out of band (npm run build in web/admin, or CI's
# frontend job) exactly like the Complement image's prebuilt binary above.
/web/admin/node_modules/
/crates/saltator-admin-ui/dist/
